# GitHub personal access token with permission to create issues on the target repo
GITHUB_TOKEN=

# Port the server listens on. Render sets this automatically in production.
PORT=3000

# Origin allowed to call this API (your deployed frontend URL in production)
CORS_ORIGIN=http://127.0.0.1:5500